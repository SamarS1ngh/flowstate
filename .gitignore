analyzer/.venv/
analyzer/out/
analyzer/auth/
# Deterministically regenerated by `python scripts/gen_fixtures.py --generate`;
# not committed to keep the golden-fixture commit lean. The JSON fixtures
# (analyzer/fixtures/*.json) are the actual oracle data and ARE committed.
analyzer/fixtures/wav/
# Deterministically regenerated by `python scripts/make_device_fixtures.py`
# from analyzer/fixtures/*.json + fixtures/wav/ -- device-pushable PCM +
# manifest for the Plan D Task 5 on-device parity harness. Same rationale as
# fixtures/wav/ above: regenerable, not the oracle data.
analyzer/device_fixtures/

app/node_modules/
app/android/app/build/
app/android/.gradle/
app/android/build/
app/android/app/*.keystore
