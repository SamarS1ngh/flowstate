analyzer/.venv/
analyzer/out/
analyzer/auth/
# Deterministically regenerated by `python scripts/gen_fixtures.py --generate`;
# not committed to keep the golden-fixture commit lean. The JSON fixtures
# (analyzer/fixtures/*.json) are the actual oracle data and ARE committed.
analyzer/fixtures/wav/

app/node_modules/
app/android/app/build/
app/android/.gradle/
app/android/build/
app/android/app/*.keystore
