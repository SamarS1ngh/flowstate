[pytest]
markers =
    integration: needs essentia models + ffmpeg (run in Docker)
    tflite: needs a tensorflow/tflite install (run in the native arm64 python:3.10 container)
addopts = -m "not integration"
